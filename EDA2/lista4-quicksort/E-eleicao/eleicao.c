#include <stdio.h>
#include <stdlib.h>

int main(void){
    int s, f, e;
    int cand;

    scanf("%d %d %d", &s, &f, &e);

    while (scanf("%d", cand) != EOF){
        /* code */
    }
    

    return 0;
}